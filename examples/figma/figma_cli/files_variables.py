#!/usr/bin/env python3
# Copyright 2026
#
# This code was generated by the openapi-spec-tools CLI generator, DO NOT EDIT
#

from datetime import date  # noqa: F401
from datetime import datetime  # noqa: F401
from enum import Enum  # noqa: F401
from pathlib import Path
from typing import Annotated  # noqa: F401
from typing import Optional  # noqa: F401

import typer
from rich_objects import display

from figma_cli import _arguments as _a
from figma_cli import _display as _d  # noqa: F401
from figma_cli import _exceptions as _e  # noqa: F401
from figma_cli import _logging as _l  # noqa: F401
from figma_cli import _requests as _r  # noqa: F401
from figma_cli import _tree as _t


app = typer.Typer(no_args_is_help=True, help="Manage files variables")


@app.command("commands", short_help="Display commands tree for variables sub-commands")
def show_commands(
    display: _a.TreeDisplayOption = _a.TreeDisplay.HELP,
    depth: _a.MaxDepthOption = 5,
    search: _a.TreeSearchOption = None,
) -> None:
    """Show variables sub-commands.

    The '*' denotes a sub-command with other sub-commands, but no direct actions.
    """
    path = Path(__file__).parent / "tree.yaml"
    _t.tree(path.as_posix(), "files_variables", display, depth, search)
    return


class VariableCollectionCreate(str, Enum):  # noqa: F811
    CREATE = "CREATE"


class VariableModeCreate(str, Enum):  # noqa: F811
    CREATE = "CREATE"


class VariableCreate(str, Enum):  # noqa: F811
    CREATE = "CREATE"


@app.command("create", short_help="Create/modify/delete variables")
def post_variables(
    file_key: Annotated[str, typer.Argument(show_default=False, help="File to modify variables in. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key.")],
    variable_collections_action: Annotated[Optional[VariableCollectionCreate], typer.Option(show_default=False, case_sensitive=False, help="The action to perform for the variable collection.")] = None,
    variable_collections_id: Annotated[Optional[str], typer.Option(show_default=False, help="A temporary id for this variable collection.")] = None,
    variable_collections_name: Annotated[Optional[str], typer.Option(show_default=False, help="The name of this variable collection.")] = None,
    variable_collections_initial_mode_id: Annotated[Optional[str], typer.Option(show_default=False, help="The initial mode refers to the mode that is created by default. You can set a temporary id here, in order to reference this mode later in this request.")] = None,
    variable_collections_hidden_from_publishing: Annotated[Optional[bool], typer.Option("--variable-collections-hidden-from-publishing/--no-variable-collections-hidden-from-publishing", help="Whether this variable collection is hidden when publishing the current file as a library.")] = False,
    variable_collections_parent_variable_collection_id: Annotated[Optional[str], typer.Option(show_default=False, help="The id of the parent variable collection that this variable collection is extending from.")] = None,
    variable_modes_action: Annotated[Optional[VariableModeCreate], typer.Option(show_default=False, case_sensitive=False, help="The action to perform for the variable mode.")] = None,
    variable_modes_id: Annotated[Optional[str], typer.Option(show_default=False, help="A temporary id for this variable mode.")] = None,
    variable_modes_name: Annotated[Optional[str], typer.Option(show_default=False, help="The name of this variable mode.")] = None,
    variable_modes_variable_collection_id: Annotated[Optional[str], typer.Option(show_default=False, help="The variable collection that will contain the mode. You can use the temporary id of a variable collection. New modes cannot be created on extended collections.")] = None,
    variables_action: Annotated[Optional[VariableCreate], typer.Option(show_default=False, case_sensitive=False, help="The action to perform for the variable.")] = None,
    variables_id: Annotated[Optional[str], typer.Option(show_default=False, help="A temporary id for this variable.")] = None,
    variables_name: Annotated[Optional[str], typer.Option(show_default=False, help="The name of this variable.")] = None,
    variables_variable_collection_id: Annotated[Optional[str], typer.Option(show_default=False, help="The variable collection that will contain the variable. You can use the temporary id of a variable collection.")] = None,
    variables_description: Annotated[Optional[str], typer.Option(show_default=False, help="The description of this variable.")] = None,
    variables_hidden_from_publishing: Annotated[Optional[bool], typer.Option("--variables-hidden-from-publishing/--no-variables-hidden-from-publishing", help="Whether this variable is hidden when publishing the current file as a library.")] = False,
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    **This API is available to full members of Enterprise orgs with Editor seats.**

    The `POST /v1/files/:file_key/variables` endpoint lets you bulk create, update, and delete variables and variable
    collections.

    The request body supports the following 4 top-level arrays. Changes from these arrays will be applied in the below
    order, and within each array, by array order.

    - **variableCollections**: For creating, updating, and deleting variable collections
    - **variableModes**: For creating, updating, and deleting modes within variable collections
      - Each collection can have a maximum of 40 modes
      - Mode names cannot be longer than 40 characters
    - **variables**: For creating, updating, and deleting variables
      - Each collection can have a maximum of 5000 variables
      - Variable names must be unique within a collection and cannot contain certain special characters such as `.{}`
    - **variableModeValues**: For setting a variable value under a specific mode.
      - When setting aliases, a variable cannot be aliased to itself or form an alias cycle

    Temporary ids can be used to reference an object later in the same POST request body. They can be used at create time in
    the `id` property of variable collections, modes, variables, and in the `initialModeId` property of variable
    collections. They are scoped to a single request body, and must be unique within the body. The mapping of temporary ids
    to real ids is returned in the response.

    This endpoint has the following key behaviors:

    - The request body must be 4MB or less.
    - Must include an `action` property for collections, modes, and variables to tell the API whether to create, update, or
    delete the object.
    - When creating a collection, mode, or variable, you can include a temporary `id` that can be referenced in dependent
    objects in the same request. For example, you can create a new collection with the id `"my_new_collection"`. You can
    then set `variableCollectionId` to `"my_new_collection"` in new modes or variables. Temporary ids must be unique in the
    request body.
    - New collections always come with one mode. You can reference this mode by setting `initialModeId` to a temporary id in
    the request body. This is useful if you want to set values for variables in the mode in the `variableModeValues` array.
      - The `tempIdToRealId` array returns a mapping of the temporary ids in the request, to the real ids of the newly
    created objects.
    - When adding new modes or variables, default variable values will be applied, consistent with what happens in the UI.
    - Everything to be created, updated, and deleted in the request body is treated as one atomic operation. If there is any
    validation failure, you will get a 400 status code response, and no changes will be persisted.
    - You will not be able to update remote variables or variable collections. You can only update variables in the file
    where they were originally created.
    '''
    # handler for postVariables: POST /v1/files/{file_key}/variables
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key, content_type="application/json")
    url = _r.create_url(_api_host, "v1/files", file_key, "variables")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}
    body = {}
    variable_collections = {}
    variable_modes = {}
    variables = {}
    if variable_collections_action is not None:
        variable_collections["action"] = variable_collections_action
    if variable_collections_id is not None:
        variable_collections["id"] = variable_collections_id
    if variable_collections_name is not None:
        variable_collections["name"] = variable_collections_name
    if variable_collections_initial_mode_id is not None:
        variable_collections["initialModeId"] = variable_collections_initial_mode_id
    if variable_collections_hidden_from_publishing is not None:
        variable_collections["hiddenFromPublishing"] = variable_collections_hidden_from_publishing
    if variable_collections_parent_variable_collection_id is not None:
        variable_collections["parentVariableCollectionId"] = variable_collections_parent_variable_collection_id
    if variable_modes_action is not None:
        variable_modes["action"] = variable_modes_action
    if variable_modes_id is not None:
        variable_modes["id"] = variable_modes_id
    if variable_modes_name is not None:
        variable_modes["name"] = variable_modes_name
    if variable_modes_variable_collection_id is not None:
        variable_modes["variableCollectionId"] = variable_modes_variable_collection_id
    if variables_action is not None:
        variables["action"] = variables_action
    if variables_id is not None:
        variables["id"] = variables_id
    if variables_name is not None:
        variables["name"] = variables_name
    if variables_variable_collection_id is not None:
        variables["variableCollectionId"] = variables_variable_collection_id
    if variables_description is not None:
        variables["description"] = variables_description
    if variables_hidden_from_publishing is not None:
        variables["hiddenFromPublishing"] = variables_hidden_from_publishing
    # stitch together the sub-objects
    if variable_collections:
        body["variableCollections"] = variable_collections
    if variable_modes:
        body["variableModes"] = variable_modes
    if variables:
        body["variables"] = variables

    try:
        data = _r.request("POST", url, headers=headers, params=params, body=body, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("local", short_help="Get local variables")
def get_local_variables(
    file_key: Annotated[str, typer.Argument(show_default=False, help="File to get variables from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key.")],
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    **This API is available to full members of Enterprise orgs.**

    The `GET /v1/files/:file_key/variables/local` endpoint lets you enumerate local variables created in the file and remote
    variables used in the file. Remote variables are referenced by their `subscribed_id`.

    As a part of the Variables related API additions, the `GET /v1/files/:file_key` endpoint now returns a `boundVariables`
    property, containing the `variableId` of the bound variable. The `GET /v1/files/:file_key/variables/local` endpoint can
    be used to get the full variable or variable collection object.

    Note that `GET /v1/files/:file_key/variables/published` does not return modes. Instead, you will need to use the `GET
    /v1/files/:file_key/variables/local` endpoint, in the same file, to examine the mode values.
    '''
    # handler for getLocalVariables: GET /v1/files/{file_key}/variables/local
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v1/files", file_key, "variables/local")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}

    try:
        data = _r.request("GET", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("published", short_help="Get published variables")
def get_published_variables(
    file_key: Annotated[str, typer.Argument(show_default=False, help="File to get variables from. This must be a main file key, not a branch key, as it is not possible to publish from branches.")],
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    **This API is available to full members of Enterprise orgs.**

    The `GET /v1/files/:file_key/variables/published` endpoint returns the variables that are published from the given file.

    The response for this endpoint contains some key differences compared to the `GET /v1/files/:file_key/variables/local`
    endpoint:

    - Each variable and variable collection contains a `subscribed_id`.
    - Modes are omitted for published variable collections

    Published variables have two ids: an id that is assigned in the file where it is created (`id`), and an id that is used
    by subscribing files (`subscribed_id`). The `id` and `key` are stable over the lifetime of the variable. The
    `subscribed_id` changes every time the variable is modified and published. The same is true for variable collections.

    The `updatedAt` fields are ISO 8601 timestamps that indicate the last time that a change to a variable was published.
    For variable collections, this timestamp will change any time a variable in the collection is changed.
    '''
    # handler for getPublishedVariables: GET /v1/files/{file_key}/variables/published
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v1/files", file_key, "variables/published")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}

    try:
        data = _r.request("GET", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


if __name__ == "__main__":
    app()
