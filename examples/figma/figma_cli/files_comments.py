#!/usr/bin/env python3
# Copyright 2026
#
# This code was generated by the openapi-spec-tools CLI generator, DO NOT EDIT
#

from datetime import date  # noqa: F401
from datetime import datetime  # noqa: F401
from enum import Enum  # noqa: F401
from pathlib import Path
from typing import Annotated  # noqa: F401
from typing import Optional  # noqa: F401

import typer
from rich_objects import display

from figma_cli import _arguments as _a
from figma_cli import _display as _d  # noqa: F401
from figma_cli import _exceptions as _e  # noqa: F401
from figma_cli import _logging as _l  # noqa: F401
from figma_cli import _requests as _r  # noqa: F401
from figma_cli import _tree as _t
from figma_cli.files_comments_reactions import app as files_comments_reactions

app = typer.Typer(no_args_is_help=True, help="Manage files comments")
app.add_typer(files_comments_reactions, name="reactions")


@app.command("commands", short_help="Display commands tree for comments sub-commands")
def show_commands(
    display: _a.TreeDisplayOption = _a.TreeDisplay.HELP,
    depth: _a.MaxDepthOption = 5,
    search: _a.TreeSearchOption = None,
) -> None:
    """Show comments sub-commands.

    The '*' denotes a sub-command with other sub-commands, but no direct actions.
    """
    path = Path(__file__).parent / "tree.yaml"
    _t.tree(path.as_posix(), "files_comments", display, depth, search)
    return


@app.command("create", short_help="Add a comment to a file")
def post_comment(
    file_key: Annotated[str, typer.Argument(show_default=False, help="File to add comments in. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key.")],
    message: Annotated[str, typer.Option(show_default=False, help="The text contents of the comment to post.")] = None,
    comment_id: Annotated[Optional[str], typer.Option(show_default=False, help="The ID of the comment to reply to, if any. This must be a root comment. You cannot reply to other replies (a comment that has a parent_id).")] = None,
    client_meta_x: Annotated[Optional[float], typer.Option(show_default=False, help="X coordinate of the vector.")] = None,
    client_meta_y: Annotated[Optional[float], typer.Option(show_default=False, help="Y coordinate of the vector.")] = None,
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Posts a new comment on the file.
    '''
    # handler for postComment: POST /v1/files/{file_key}/comments
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key, content_type="application/json")
    url = _r.create_url(_api_host, "v1/files", file_key, "comments")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if message is None:
        missing.append("--message")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}
    body = {}
    client_meta = {}
    body["message"] = message
    if comment_id is not None:
        body["comment_id"] = comment_id
    if client_meta_x is not None:
        client_meta["x"] = client_meta_x
    if client_meta_y is not None:
        client_meta["y"] = client_meta_y
    # stitch together the sub-objects
    if client_meta:
        body["client_meta"] = client_meta

    try:
        data = _r.request("POST", url, headers=headers, params=params, body=body, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("delete", short_help="Delete a comment")
def delete_comment(
    file_key: Annotated[str, typer.Argument(show_default=False, help="File to delete comment from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key.")],
    comment_id: Annotated[str, typer.Argument(show_default=False, help="Comment id of comment to delete")],
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Deletes a specific comment. Only the person who made the comment is allowed to delete it.
    '''
    # handler for deleteComment: DELETE /v1/files/{file_key}/comments/{comment_id}
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v1/files", file_key, "comments", comment_id)
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}

    try:
        data = _r.request("DELETE", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("show", short_help="Get comments in a file")
def get_comments(
    file_key: Annotated[str, typer.Argument(show_default=False, help="File to get comments from. This can be a file key or branch key. Use `GET /v1/files/:key` with the `branch_data` query param to get the branch key.")],
    as_md: Annotated[Optional[bool], typer.Option("--as-md/--no-as-md", show_default=False, help="If enabled, will return comments as their markdown equivalents when applicable.")] = None,
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Gets a list of comments left on the file.
    '''
    # handler for getComments: GET /v1/files/{file_key}/comments
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v1/files", file_key, "comments")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}
    if as_md is not None:
        params["as_md"] = as_md

    try:
        data = _r.request("GET", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


if __name__ == "__main__":
    app()
