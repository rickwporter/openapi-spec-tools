#!/usr/bin/env python3
# Copyright 2026
#
# This code was generated by the openapi-spec-tools CLI generator, DO NOT EDIT
#

from datetime import date  # noqa: F401
from datetime import datetime  # noqa: F401
from enum import Enum  # noqa: F401
from pathlib import Path
from typing import Annotated  # noqa: F401
from typing import Optional  # noqa: F401

import typer
from rich_objects import display

from figma_cli import _arguments as _a
from figma_cli import _display as _d  # noqa: F401
from figma_cli import _exceptions as _e  # noqa: F401
from figma_cli import _logging as _l  # noqa: F401
from figma_cli import _requests as _r  # noqa: F401
from figma_cli import _tree as _t


app = typer.Typer(no_args_is_help=True, help="Manage webhooks")


@app.command("commands", short_help="Display commands tree for webhooks sub-commands")
def show_commands(
    display: _a.TreeDisplayOption = _a.TreeDisplay.HELP,
    depth: _a.MaxDepthOption = 5,
    search: _a.TreeSearchOption = None,
) -> None:
    """Show webhooks sub-commands.

    The '*' denotes a sub-command with other sub-commands, but no direct actions.
    """
    path = Path(__file__).parent / "tree.yaml"
    _t.tree(path.as_posix(), "webhooks", display, depth, search)
    return


class WebhookV2Event(str, Enum):  # noqa: F811
    PING = "PING"
    FILE_UPDATE = "FILE_UPDATE"
    FILE_VERSION_UPDATE = "FILE_VERSION_UPDATE"
    FILE_DELETE = "FILE_DELETE"
    LIBRARY_PUBLISH = "LIBRARY_PUBLISH"
    FILE_COMMENT = "FILE_COMMENT"
    DEV_MODE_STATUS_UPDATE = "DEV_MODE_STATUS_UPDATE"


class WebhookV2Status(str, Enum):  # noqa: F811
    ACTIVE = "ACTIVE"
    PAUSED = "PAUSED"


@app.command("create", short_help="Create a webhook")
def post_webhook(
    event_type: Annotated[WebhookV2Event, typer.Option(show_default=False, case_sensitive=False, help="An enum representing the possible events that a webhook can subscribe to")] = None,
    team_id: Annotated[Optional[str], typer.Option(show_default=False, hidden=True, help="Team id to receive updates about. This is deprecated, use \'context\' and \'context_id\' instead.")] = None,
    context: Annotated[str, typer.Option(show_default=False, help="Context to create the webhook for. Must be \"team\", \"project\", or \"file\".")] = None,
    context_id: Annotated[str, typer.Option(show_default=False, help="The id of the context you want to receive updates about.")] = None,
    endpoint: Annotated[str, typer.Option(show_default=False, help="The HTTP endpoint that will receive a POST request when the event triggers. Max length 2048 characters.")] = None,
    passcode: Annotated[str, typer.Option(show_default=False, help="String that will be passed back to your webhook endpoint to verify that it is being called by Figma. Max length 100 characters.")] = None,
    status: Annotated[Optional[WebhookV2Status], typer.Option(show_default=False, case_sensitive=False, help="An enum representing the possible statuses you can set a webhook to:")] = None,
    description: Annotated[Optional[str], typer.Option(show_default=False, help="User provided description or name for the webhook. Max length 150 characters.")] = None,
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Create a new webhook which will call the specified endpoint when the event triggers. By default, this webhook will
    automatically send a PING event to the endpoint when it is created. If this behavior is not desired, you can create the
    webhook and set the status to PAUSED and reactivate it later.
    '''
    # handler for postWebhook: POST /v2/webhooks
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key, content_type="application/json")
    url = _r.create_url(_api_host, "v2/webhooks")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if event_type is None:
        missing.append("--event-type")
    if context is None:
        missing.append("--context")
    if context_id is None:
        missing.append("--context-id")
    if endpoint is None:
        missing.append("--endpoint")
    if passcode is None:
        missing.append("--passcode")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}
    body = {}
    body["event_type"] = event_type
    if team_id is not None:
        _l.logger().warning("--team-id is deprecated and should not be used")
        body["team_id"] = team_id
    body["context"] = context
    body["context_id"] = context_id
    body["endpoint"] = endpoint
    body["passcode"] = passcode
    if status is not None:
        body["status"] = status
    if description is not None:
        body["description"] = description

    try:
        data = _r.request("POST", url, headers=headers, params=params, body=body, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("delete", short_help="Delete a webhook")
def delete_webhook(
    webhook_id: Annotated[str, typer.Argument(show_default=False, help="ID of webhook to delete")],
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Deletes the specified webhook. This operation cannot be reversed.
    '''
    # handler for deleteWebhook: DELETE /v2/webhooks/{webhook_id}
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v2/webhooks", webhook_id)
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}

    try:
        data = _r.request("DELETE", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("list", short_help="Get webhooks by context or plan")
def get_webhooks(
    context: Annotated[Optional[str], typer.Option(show_default=False, help="Context to create the resource on. Should be \"team\", \"project\", or \"file\".")] = None,
    context_id: Annotated[Optional[str], typer.Option(show_default=False, help="The id of the context that you want to get attached webhooks for. If you\'re using context_id, you cannot use plan_api_id.")] = None,
    plan_api_id: Annotated[Optional[str], typer.Option(show_default=False, help="The id of your plan. Use this to get all webhooks for all contexts you have access to. If you\'re using plan_api_id, you cannot use context or context_id. When you use plan_api_id, the response is paginated.")] = None,
    cursor: Annotated[Optional[str], typer.Option(show_default=False, help="If you\'re using plan_api_id, this is the cursor to use for pagination. If you\'re using context or context_id, this parameter is ignored. Provide the next_page or prev_page value from the previous response to get the next or previous page of results.")] = None,
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Returns a list of webhooks corresponding to the context or plan provided, if they exist. For plan, the webhooks for all
    contexts that you have access to will be returned, and theresponse is paginated
    '''
    # handler for getWebhooks: GET /v2/webhooks
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v2/webhooks")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}
    if context is not None:
        params["context"] = context
    if context_id is not None:
        params["context_id"] = context_id
    if plan_api_id is not None:
        params["plan_api_id"] = plan_api_id
    if cursor is not None:
        params["cursor"] = cursor

    try:
        data = _r.request("GET", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("requests", short_help="Get webhook requests")
def get_webhook_requests(
    webhook_id: Annotated[str, typer.Argument(show_default=False, help="The id of the webhook subscription you want to see events from")],
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Returns all webhook requests sent within the last week. Useful for debugging.
    '''
    # handler for getWebhookRequests: GET /v2/webhooks/{webhook_id}/requests
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v2/webhooks", webhook_id, "requests")
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}

    try:
        data = _r.request("GET", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


class WebhookV2Event(str, Enum):  # noqa: F811
    PING = "PING"
    FILE_UPDATE = "FILE_UPDATE"
    FILE_VERSION_UPDATE = "FILE_VERSION_UPDATE"
    FILE_DELETE = "FILE_DELETE"
    LIBRARY_PUBLISH = "LIBRARY_PUBLISH"
    FILE_COMMENT = "FILE_COMMENT"
    DEV_MODE_STATUS_UPDATE = "DEV_MODE_STATUS_UPDATE"


class WebhookV2Status(str, Enum):  # noqa: F811
    ACTIVE = "ACTIVE"
    PAUSED = "PAUSED"


@app.command("set", short_help="Update a webhook")
def put_webhook(
    webhook_id: Annotated[str, typer.Argument(show_default=False, help="ID of webhook to update")],
    event_type: Annotated[WebhookV2Event, typer.Option(show_default=False, case_sensitive=False, help="An enum representing the possible events that a webhook can subscribe to")] = None,
    endpoint: Annotated[str, typer.Option(show_default=False, help="The HTTP endpoint that will receive a POST request when the event triggers. Max length 2048 characters.")] = None,
    passcode: Annotated[str, typer.Option(show_default=False, help="String that will be passed back to your webhook endpoint to verify that it is being called by Figma. Max length 100 characters.")] = None,
    status: Annotated[Optional[WebhookV2Status], typer.Option(show_default=False, case_sensitive=False, help="An enum representing the possible statuses you can set a webhook to:")] = None,
    description: Annotated[Optional[str], typer.Option(show_default=False, help="User provided description or name for the webhook. Max length 150 characters.")] = None,
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Update a webhook by ID.
    '''
    # handler for putWebhook: PUT /v2/webhooks/{webhook_id}
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key, content_type="application/json")
    url = _r.create_url(_api_host, "v2/webhooks", webhook_id)
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if event_type is None:
        missing.append("--event-type")
    if endpoint is None:
        missing.append("--endpoint")
    if passcode is None:
        missing.append("--passcode")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}
    body = {}
    body["event_type"] = event_type
    body["endpoint"] = endpoint
    body["passcode"] = passcode
    if status is not None:
        body["status"] = status
    if description is not None:
        body["description"] = description

    try:
        data = _r.request("PUT", url, headers=headers, params=params, body=body, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


@app.command("show", short_help="Get a webhook")
def get_webhook(
    webhook_id: Annotated[str, typer.Argument(show_default=False, help="ID of webhook to get")],
    _api_host: _a.ApiHostOption = "https://api.figma.com",
    _api_key: _a.ApiKeyOption = None,
    _api_timeout: _a.ApiTimeoutOption = 5,
    _log_level: _a.LogLevelOption = _a.LogLevel.WARN,
    _out_fmt: _a.OutputFormatOption = _a.OutputFormat.TABLE,
    _out_style: _a.OutputStyleOption = _a.OutputStyle.ALL,
) -> None:
    '''
    Get a webhook by ID.
    '''
    # handler for getWebhook: GET /v2/webhooks/{webhook_id}
    _l.init_logging(_log_level)
    headers = _r.request_headers(_api_key)
    url = _r.create_url(_api_host, "v2/webhooks", webhook_id)
    missing = []
    if _api_key is None:
        missing.append("--api-key")
    if missing:
        _e.handle_exceptions(_e.MissingRequiredError(missing))

    params = {}

    try:
        data = _r.request("GET", url, headers=headers, params=params, timeout=_api_timeout)
        display(data, _out_fmt, _out_style)
    except Exception as ex:
        _e.handle_exceptions(ex)

    return


if __name__ == "__main__":
    app()
